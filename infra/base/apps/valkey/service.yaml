apiVersion: v1
kind: Service
metadata:
  name: valkey-infra-redis-master
  namespace: valkey
  labels:
    app.kubernetes.io/name: redis
spec:
  selector:
    app.kubernetes.io/name: redis
  ports:
    - port: 6379
      targetPort: 6379
